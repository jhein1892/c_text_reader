// Implement a basic command-line application that reads data from a text file and performs operations such as word count, line count, and character count.

#include <stdio.h>

int main(){
    // Ask user for Text file
        // Start with just having a text file in this folder
    // Go into the text file

    // Count the words
    
    // Count the lines

    // Count the characters (excluding spaces and punctuation)
}